using UnityEngine;
using System.Collections;

public class TimeObject : MonoBehaviour {

	float TimeSpeed = 3f;

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
		//transform.Rotate(transform.rotation.x + TimeSpeed * Time.deltaTime, transform.rotation.y + TimeSpeed * Time.deltaTime, transform.rotation.z + TimeSpeed * Time.deltaTime);
	}
}
